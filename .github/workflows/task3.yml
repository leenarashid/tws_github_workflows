name: Self-hosted Runner

on: 
  push:
    branches: [main]

jobs:
  example-job:
    runs-on: ubuntu-latest   # GitHub-hosted runner
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Print OS
        run: uname -a
