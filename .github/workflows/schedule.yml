---
name: Daily Workflow
on:
  workflow_dispatch:
  schedule:
    - cron: 0 0 * * *
jobs:
  schedule_update:
    runs-on: ubuntu-latest
    steps:
      - name: Print Schedule message
        run: |
          echo "Workflow running at midnight UTC"
          date

