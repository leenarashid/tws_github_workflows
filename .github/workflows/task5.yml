name: Label Runner
on:
  push:
    branches: [main]
jobs:
  label-runner:
    runs-on: [self-hosted, ubuntu, my linux runner]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Print Hostname
        run: hostname
